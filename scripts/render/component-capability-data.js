const {
  CAPABILITY_ROWS
} = require('./component-capability-rows');
const {
  CHART_COMPONENT_ID_LIST,
  COMPONENT_ALIASES,
  COMPONENT_DATA_REQUIREMENTS
} = require('./component-capability-contracts');

const COMPONENT_MANIFEST_VERSION = 'component-capability-manifest/v1';

module.exports = {
  CAPABILITY_ROWS,
  CHART_COMPONENT_ID_LIST,
  COMPONENT_ALIASES,
  COMPONENT_DATA_REQUIREMENTS,
  COMPONENT_MANIFEST_VERSION
};
